const ParameterConfiguration = () => {
  return (
    <div style={{ flex: 1, border: '1px solid #444' }}>
      <div className='header'>
        Configure Parameters
      </div>
      <div style={{ padding: '10px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        select and edit pipeline parameters
      </div>
    </div>
  );
};

export default ParameterConfiguration;